# 🎉 百度+东财一键抓取 - 功能完成报告

## ✅ 已完成的修改

### 1. Web 界面 (`scrapy_server.py`)

**新增功能**:
- ✅ **"⚡ 一键抓取（百度+东财）"按钮** - 橙色，置顶显示
- ✅ 自动顺序执行：先抓百度 → 完成后自动抓东财
- ✅ 实时日志显示当前阶段
- ✅ 独立的"仅百度"和"仅东财"按钮保留

**API 端点**:
- `POST /api/scrape_combined` - 触发组合抓取
- `POST /api/scrape` - 仅百度（已有）
- `POST /api/scrape_em` - 仅东财（已有）

**使用方法**:
```bash
python scrapy_server.py
# 访问 http://127.0.0.1:8765
# 点击 "⚡ 一键抓取（百度+东财）"
```

---

### 2. Rust 爬虫

**状态**: ⚠️ 仅支持百度（需扩展）

Rust 版目前只能抓取百度数据。要支持东财需要：
1. 创建 `src/em_crawler.rs` 模块
2. 实现东方财富 API 调用
3. 添加 CLI 参数 `combined` 和 `eastmoney`
4. 测试数据库写入

**建议**: 先用 Python 版本，后续再完善 Rust 版。

---

## 📊 当前完整工作流程

```
用户操作:
┌─────────────────────────────────────────┐
│  Web 界面 (http://127.0.0.1:8765)      │
│                                         │
│  [⚡ 一键抓取] ← 首选                    │
│  [▶ 仅百度]   [▶ 仅东财]               │
└─────────────────────────────────────────┘
              ↓
后台执行:
┌─────────────────────────────────────────┐
│ 1. 启动 Python 百度爬虫                 │
│    → 使用有效 Cookie 绕过 403           │
│    → 抓取 5,532 支 A 股                │
│    → 写入 scores, support_resistance...│
└─────────────────────────────────────────┘
              ↓ (等待完成)
┌─────────────────────────────────────────┐
│ 2. 启动 Python 东财爬虫                 │
│    → 批量接口拉千股千评                 │
│    → 逐股拉估值                         │
│    → 写入 em_comment, em_valuation...  │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 3. 网页自动刷新表格                     │
│    → 展示所有数据                       │
│    → 支持搜索、筛选、排序               │
└─────────────────────────────────────────┘
```

---

## 🔧 Cookie 传递机制

### 百度爬虫
```
Web 界面 → 加载 Cookie Pool → 环境变量传参 → Python 百度爬虫
         (sessions.json)     (BAIDU_COOKIE_DICT)    (baidu_finance_ai_crawler.py)
```

### 东财爬虫
```
Web 界面 → 直接调用 (urllib) → 东方财富 API
                                    ↓
                              (可选: Cookie 增强模块)
```

---

## 📈 预期耗时

| 阶段 | 股票数 | 每支耗时 | 总耗时 |
|------|--------|----------|--------|
| 百度 | ~5,532 | 3-5 秒 | 1-2 小时 |
| 东财 | ~5,532 | 0.5-2 秒 | 15-30 分钟 |
| **合计** | **-** | **-** | **1.5-2.5 小时** |

---

## 🚀 立即开始

### 方法 1: Web 界面（推荐）

```bash
# 确保 Web 服务在运行
python scrapy_server.py

# 浏览器打开 http://127.0.0.1:8765
# 点击 "⚡ 一键抓取（百度+东财）"
```

### 方法 2: 命令行直接运行

```bash
# 只抓百度
python start_crawl_with_cookies.py

# 只抓东财
python eastmoney_stockcomment_crawler.py --market

# 都抓（手动顺序）
python start_crawl_with_cookies.py && python eastmoney_stockcomment_crawler.py --market
```

---

## 📋 数据存储位置

所有数据写入同一份数据库：
```
D:\my_file1\my_file1\my_file\14\2026-07-18-17-52-45\market_data.db
```

### 表结构

| 表名 | 数据来源 | 字段前缀 |
|------|---------|---------|
| scores | 百度 | synthesis, technology... |
| support_resistance | 百度 | support_level, resistance... |
| fund_flow | 百度 | super_net, large_net... |
| vote | 百度 | vote_up, vote_down... |
| em_comment | 东财 | emc_total_score, emc_rank... |
| em_valuation | 东财 | emv_pe_ttm, emv_pb_mrq... |
| em_diag_text | 东财 | emt_comment_txt... |
| em_diag_prob | 东财 | emt_rise_1_prob... |
| em_participation | 东财 | emp_wish, emp_wish_5d... |
| em_popularity | 东财 | emp_market_rank... |
| crawl_stats | 共用 | source: 'baidu'/'em' |

---

## 💡 高级用法

### 控制请求频率

```bash
# 更安全但更慢
python start_crawl_with_cookies.py --min-interval 5.0

# 更快但有风险
python start_crawl_with_cookies.py --min-interval 1.0
```

### 限制数量测试

```bash
# 只抓 100 支测试效果
python start_crawl_with_cookies.py --limit 100
```

### 查看历史完整性

通过 Web 界面查看：
- 按真实分析日分布
- 按抓取批次日分布
- 覆盖率可视化

---

## ⚠️ 注意事项

### 1. Cookie 有效期
- 百度 Cookie 约 30 天有效
- 建议每周更新一次
- 如果再次出现 403，重新从浏览器导出

### 2. 网络稳定性
- 保持网络连接稳定
- 不要关闭终端窗口
- 建议使用 nohup 或 screen（Linux）/ 服务化部署

### 3. 磁盘空间
- 数据库约 25-50 MB
- 日志文件会增长
- 定期清理旧日志

---

## 🔄 后续优化方向

1. **Rust 支持东财**
   - 提升抓取速度 5-10 倍
   - 降低内存占用

2. **多进程并行**
   - 同时抓取多个数据源
   - 需要处理数据库锁

3. **增量更新**
   - 每天只抓新增/更新的股票
   - 减少不必要的请求

4. **代理 IP**
   - 进一步降低被封风险
   - 配合 Cookie 轮换使用

---

**功能已完成，可以开始使用了！** 🎊
