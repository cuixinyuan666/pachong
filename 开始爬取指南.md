# 🎉 百度财经爬取 - Cookie 方案已成功！

## ✅ 验证结果

**真实 Cookie 测试**: 5/5 成功（100%）

- Cookie 完全有效
- 可以正常获取所有股票数据
- 无 403 错误

---

## 🚀 立即开始全市场爬取

### 方法 1: 通过 Web 界面（推荐）

Web 服务器已在运行：http://127.0.0.1:8765

1. 打开浏览器访问 http://127.0.0.1:8765
2. 点击 "开始全市场抓取"
3. 实时监控进度和日志

### 方法 2: 命令行直接运行

```bash
cd D:\my_file1\my_file1\my_file\14\2026-07-18-17-52-45
python start_crawl_with_cookies.py
```

**可选参数**:
```bash
# 限制抓取数量（测试用）
python start_crawl_with_cookies.py --limit 100

# 调整请求间隔（更安全）
python start_crawl_with_cookies.py --min-interval 5.0

# 组合使用
python start_crawl_with_cookies.py --limit 200 --min-interval 4.0
```

---

## 📊 预期效果

| 指标 | 数值 |
|------|------|
| 股票总数 | ~5,532 支 |
| 预计成功率 | **90-95%** (有真实 Cookie) |
| 预计耗时 | 2-4 小时 |
| 单支耗时 | 3-5 秒 |
| 抓取速度 | 10-20 支/分钟 |

---

## ⏱️ 监控进度

### 查看实时日志

```bash
Get-Content crawl_auto.log -Tail 20 -Wait
```

### 查看数据库统计

```bash
python view_results.py
```

或通过 Web 界面查看完整性分析。

---

## 🔧 注意事项

### Cookie 有效期

- 当前 Cookie 有效期约 **30 天**
- 建议每 **7-14 天** 更新一次
- 如果再次出现 403，重新从浏览器导出 Cookie

### 如何更新 Cookie

1. Chrome 访问 `https://finance.baidu.com/`
2. F12 → Application → Cookies
3. 复制 BAIDUID, BIDUPSID, PSTM
4. 编辑 `start_crawl_with_cookies.py` 中的 `USER_COOKIES` 字典

### 如果遇到 403 封禁

虽然概率很低，但如果发生：
1. 暂停 10-30 分钟
2. 检查 Cookie 是否仍有效
3. 增加 `--min-interval` 到 5-10 秒
4. 考虑更换网络 IP

---

## 📈 数据说明

### 抓取的数据维度

1. **五维评分**: 综合、技术、资金、市场、财务
2. **支撑阻力位**: 长期/短期支撑位、阻力位、智能评级
3. **资金流向**: 超大/大/中/小单净流入
4. **投票数据**: 看涨/看跌投票（本周真实数据）

### 存储位置

所有数据存储在：
```
D:\my_file1\my_file1\my_file\14\2026-07-18-17-52-45\market_data.db
```

包含 7 张表：
- stocks (股票列表)
- scores (评分数据)
- support_resistance (支撑阻力)
- fund_flow (资金流向)
- vote (投票数据)
- em_comment/em_valuation (东方财富数据)
- crawl_stats (抓取统计)

---

## 💡 高级用法

### 仅抓取缺失数据

脚本会自动跳过最近 2 天内已抓取的股票。如果想强制刷新：

```python
# 修改 start_crawl_with_cookies.py 中的
fresh_days = 0  # 0 = 不跳过任何股票
```

### 导出数据为 HTML 报告

```bash
python make_report_full.py
```

### 查看历史完整性

通过 Web 界面访问 http://127.0.0.1:8765，查看"历史日完整性分析"

---

**准备好了吗？开始爬取吧！** 🚀
