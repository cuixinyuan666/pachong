# 百度 vs 东方财富爬虫 - 逻辑统一方案

## 📋 当前状态分析

### 百度爬虫 (`baidu_finance_ai_crawler.py`)
- **数据源**: `https://finance.pae.baidu.com/vapi/v1/analysis`
- **反爬**: ⭐⭐⭐⭐⭐ 极强（403 频繁）
- **Cookie 支持**: ✅ 已集成真实 Cookie
- **存储表**: scores, support_resistance, fund_flow, vote

### 东财爬虫 (`eastmoney_stockcomment_crawler.py`)  
- **数据源**: `https://datacenter-web.eastmoney.com/api/data/v1/get`
- **反爬**: ⭐⭐ 较弱
- **Cookie 支持**: ✅ 可选（防御性增强）
- **存储表**: em_comment, em_valuation, em_diag_text, em_diag_prob

### Web 界面 (`scrapy_server.py`)
- **百度抓取**: 调用 `baidu_finance_ai_crawler.py --market`
- **东财抓取**: 调用 `eastmoney_stockcomment_crawler.py --market`
- **API 端点**:
  - `/api/scrape` → 触发百度爬取
  - `/api/scrape_em` → 触发东财爬取
- **数据查看**: 读取 `market_data.db` 实时展示

### Rust 爬虫 (`rust_crawler/`)
- **数据源**: 与百度相同 API
- **反爬**: 内置相同策略
- **Cookie 支持**: ❌ 未集成
- **存储表**: stocks, scores, support_resistance, fund_flow, vote

---

## 🔍 问题识别

**当前使用不同抓取逻辑和接口，存在以下不一致：**

| 维度 | Python 百度 | Rust 百度 | 东财 Python |
|------|------------|-----------|-------------|
| 接口路径 | `/vapi/v1/analysis` | 同左 | `/api/data/v1/get` |
| Cookie 管理 | ✅ 有 | ❌ 无 | ✅ 可选 |
| 限流器 | RateLimiter (1-2s) | 内置配置 | RateLimiter (0.1-0.5s) |
| 重试策略 | 指数退避 (3次) | 内置配置 | 指数退避 (3次) |
| 数据解析 | 函数级 | struct 级 | dict 级 |
| 续跑逻辑 | skip_recent_ok | 内置断点 | skip_recent_ok |

---

## ✅ 统一方案

### 方案 A: 创建共享抓取核心模块（推荐）

**核心思路**: 提取公共逻辑到一个共享库，百度、东财、Rust 都调用同一套核心函数。

**目录结构**:
```
14/2026-07-18-17-52-45/
├── core/                    # [新增] 共享抓取核心
│   ├── __init__.py
│   ├── http_client.py      # HTTP 客户端（统一请求）
│   ├── rate_limiter.py     # 限流器
│   ├── cookie_pool.py      # Cookie 管理（复用现有）
│   └── parser.py           # 数据解析器
├── baidu_crawler.py         # 复用核心 + 百度特定逻辑
├── eastmoney_crawler.py     # 复用核心 + 东财特定逻辑
├── rust_crawler/src/core.rs # Rust 版本也调用统一逻辑
└── scrapy_server.py         # Web 界面
```

**优势**:
- ✅ 避免代码重复
- ✅ 统一错误处理
- ✅ 易于维护升级
- ✅ Cookie/限流等逻辑只改一处

### 方案 B: 保持现状，仅增强各模块（快速实现）

**核心思路**: 不重构，仅确保各模块行为一致。

**需要做的修改**:
1. **Rust 爬虫添加 Cookie 支持**
2. **Web 界面传递 Cookie 给爬虫**
3. **统一日志格式和进度输出**

---

## 🚀 实施方案 B（推荐 - 快速见效）

### Step 1: 统一 Cookie 管理

修改所有爬虫都使用同一个 Cookie 管理器。

**百度**: 已实现 ✅
**东财**: 已实现 ✅
**Rust**: 待实现

### Step 2: 统一限流配置

```python
# 所有爬虫统一参数
MIN_INTERVAL = 2.0        # 最小请求间隔
MAX_PER_MINUTE = 25       # 每分钟上限
JITTER = 0.5              # 随机抖动
CONSEC_403_THRESHOLD = 8  # 连续 403 阈值
COOLDOWN_SEC = 900        # 冷却时间 (15分钟)
```

### Step 3: Web 界面集成 Cookie

修改 `scrapy_server.py` 传递 Cookie 给爬虫进程：

```python
def start_scrape() -> tuple[bool, str]:
    # 加载 Cookie
    from baidu_cookie_pool import get_cookie_pool
    pool = get_cookie_pool()
    session = pool.get_best_session()
    
    # 写入环境变量供爬虫读取
    if session.cookies:
        os.environ["BAIDU_COOKIES"] = json.dumps(session.cookies)
    
    proc = subprocess.Popen(
        [sys.executable, SCRIPT_PATH, "--market"],
        env=os.environ,  # 传递 Cookie
    )
```

### Step 4: 统一数据库 Schema

确保 Rust 和 Python 写入完全相同的字段和类型。

---

## 📊 当前逻辑对比矩阵

| 功能 | Python 百度 | Python 东财 | Rust 百度 | Web 界面 |
|------|------------|-------------|-----------|----------|
| Cookie 轮换 | ✅ | ✅(可选) | ❌ | N/A |
| 限流器 | ✅ | ✅ | ✅ | N/A |
| 403 自愈 | ✅ | ✅ | ✅ | N/A |
| 断点续跑 | ✅ | ✅ | ✅ | N/A |
| 实时日志 | ✅ | ✅ | ✅ | ✅ |
| 进度显示 | [prog] | [prog] | GUI/CLI | 实时刷新 |
| 数据存储 | SQLite | SQLite | SQLite | 只读查看 |
| 支持增量更新 | ✅ | ✅ | ✅ | N/A |

---

## 💡 建议立即执行的改进

### 1. Web 界面传递 Cookie（5 分钟）

修改 `scrapy_server.py` 的 `start_scrape()` 函数：

```python
def start_scrape() -> tuple[bool, str]:
    with STATE_LOCK:
        if STATE["running"]:
            return False, "已有任务在运行"
        STATE["running"] = True
    
    # 加载 Cookie
    from baidu_cookie_pool import get_cookie_pool
    pool = get_cookie_pool()
    session = pool.get_best_session()
    
    env = dict(os.environ)
    env["PYTHONUNBUFFERED"] = "1"
    
    # 如果有 Cookie，写入环境变量
    if session and session.cookies:
        env["_BAIDU_COOKIE_DICT"] = json.dumps(session.cookies)
    
    proc = subprocess.Popen(
        [sys.executable, SCRIPT_PATH, "--market", "--progress-log"],
        cwd=HERE, env=env,
        stdout=subprocess.PIPE, stderr=subprocess.STDOUT,
        text=True, bufsize=1,
    )
    ...
```

然后在 `baidu_finance_ai_crawler.py` 中读取：

```python
import os
if "_BAIDU_COOKIE_DICT" in os.environ:
    cookies = json.loads(os.environ["_BAIDU_COOKIE_DICT"])
    # 使用这些 Cookie
```

### 2. 统一错误日志格式

所有爬虫输出相同格式的错误信息，便于 Web 界面解析。

### 3. Rust 添加 Cookie 支持（中期目标）

Rust 版可以通过读取环境变量获取 Cookie，并在 HTTP 请求中添加。

---

## ⏰ 执行计划

### 立即执行（今天）
- [x] Python 百度添加 Cookie 支持
- [x] Python 东财添加 Cookie 支持
- [ ] Web 界面传递 Cookie 给百度爬虫
- [ ] Web 界面传递 Cookie 给东财爬虫

### 短期（本周）
- [ ] Rust 爬虫读取 Cookie 环境变量
- [ ] 统一限流器默认配置
- [ ] 统一错误码和日志格式

### 中期（下周）
- [ ] 提取核心逻辑到共享库
- [ ] 实现统一配置管理
- [ ] 添加健康检查和监控

---

**总结**: 
- **Python 版**: 已统一，都有 Cookie 支持 ✅
- **Rust 版**: 待添加 Cookie 支持 ⚠️  
- **Web 界面**: 需修改以传递 Cookie ⚠️

需要我现在开始实施 Web 界面传递 Cookie 吗？
